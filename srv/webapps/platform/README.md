# Legacy Platform App (Retired)

This directory is intentionally kept as a stub. The platform now lives under
`/srv/compose/platform` (Flask BFF + Keycloak + Postgres). Static client sites
remain under `/srv/webapps/clients`.
